# Base URL of the backend API. During `npm run dev`, requests to /api are
# proxied to http://localhost:4000 automatically (see vite.config.js), so this
# is only needed for a production build served separately from the API.
VITE_API_URL=/api
