# Keep the raw schema file from being downloaded directly; seed.php still runs fine.
<Files "database.sql">
  <IfModule mod_authz_core.c>
    Require all denied
  </IfModule>
  <IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
  </IfModule>
</Files>
